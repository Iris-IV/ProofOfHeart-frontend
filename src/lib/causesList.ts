/** Number of cause cards rendered per page on /causes (load-more pagination). */
export const CAUSES_PAGE_SIZE = 12;
export const CAUSES_EMPTY_STATE_MESSAGE = 'No causes found. Try adjusting your filters or clearing all filters.';
