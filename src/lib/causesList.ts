/**
 * Number of cause cards rendered per page on /causes (load-more pagination).
 * @deprecated Use DEFAULT_CAMPAIGNS_PAGE_SIZE from contractClient instead.
 * Kept here to avoid breaking any external references; value kept in sync.
 */
export const CAUSES_PAGE_SIZE = 20;
