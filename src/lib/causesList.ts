/** Number of cause cards rendered per page on /causes (load-more pagination). */
export const CAUSES_PAGE_SIZE = 12;

/** Number of campaigns fetched per chunk when using infinite-query pagination. */
export const CAMPAIGNS_CHUNK_SIZE = 20;
